{
  "name": "onwork",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "check-types": "pnpm -r check-types",
    "dev:native": "pnpm --filter native dev",
    "dev:web": "pnpm --filter web dev",
    "dev:server": "pnpm --filter server dev",
    "db:push": "pnpm --filter @onwork/db db:push",
    "db:studio": "pnpm --filter @onwork/db db:studio",
    "db:generate": "pnpm --filter @onwork/db db:generate",
    "db:migrate": "pnpm --filter @onwork/db db:migrate"
  },
  "packageManager": "pnpm@10.8.0",
  "dependencies": {
    "dotenv": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "@types/bun": "catalog:"
  }
}
